<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BTL_HeThongDatSanCauLong">
        <activity android:name=".controllers.MainHomeActivity" />
        <activity android:name=".controllers.FeaturedActivity" />
        <activity android:name=".controllers.CourtDetailActivity" />
        <activity android:name=".controllers.VisualBookingActivity" />
        <activity android:name=".controllers.BookingDetailActivity" />
        <activity android:name=".controllers.AccountActivity"/>
        <activity android:name=".controllers.MapActivity"/>
        <activity
            android:name=".controllers.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>